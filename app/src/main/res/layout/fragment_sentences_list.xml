<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_sentences_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.dyszlewskiR.edu.scientling.presentation.fragment.SentencesListFragment"
    tools:showIn="@layout/activity_sentences_list"
    android:background="@drawable/shadow_border"
    >
    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/list"
        android:focusable="false"
        android:layout_above="@+id/ok_button">
    </ListView>
    <Button
        style="@style/ButtonStyle"
        android:id="@+id/ok_button"
        android:layout_alignParentBottom="true"
        android:text="@string/ok"
        android:layout_marginTop="@dimen/margin"/>
</RelativeLayout>
